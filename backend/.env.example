# PostgreSQL connection
DATABASE_URL=postgresql://localhost:5432/psk_inventory

# JWT
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=12h

# Server
PORT=4000

# Business config
# Credit breach behaviour: warn (allow override) | block (hard reject)
CREDIT_BREACH_MODE=warn
